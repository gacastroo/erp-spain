spring:
  application:
    name: erp-spain

  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3307/erp_spain?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Europe/Madrid&useUnicode=true&characterEncoding=utf8}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root}
    hikari:
      maximum-pool-size: ${DB_POOL_MAX_SIZE:10}
      minimum-idle: ${DB_POOL_MIN_IDLE:2}
      connection-timeout: 10000
      validation-timeout: 3000
      idle-timeout: 600000
      max-lifetime: 1800000

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        jdbc:
          time_zone: Europe/Madrid
          batch_size: 30
        order_inserts: true
        order_updates: true
        format_sql: false
        query:
          in_clause_parameter_padding: true
    show-sql: false

  flyway:
    enabled: true
    locations: classpath:db/migration
    # V9 was an old demo-only migration and is intentionally absent from this location.
    ignore-migration-patterns: versioned:missing

  thymeleaf:
    cache: ${THYMELEAF_CACHE:false}

  web:
    resources:
      cache:
        cachecontrol:
          max-age: ${STATIC_CACHE_MAX_AGE:1h}
          cache-public: true
      chain:
        cache: true
        compressed: true
        strategy:
          content:
            enabled: true
            paths:
              - /**

server:
  port: 8080
  forward-headers-strategy: framework
  compression:
    enabled: true
    mime-types: text/html,text/xml,text/plain,text/css,text/javascript,application/javascript,application/json,application/xml,image/svg+xml
    min-response-size: 1024
  servlet:
    session:
      timeout: 30m
      cookie:
        http-only: true
        same-site: lax
        secure: ${SESSION_COOKIE_SECURE:false}
  error:
    include-message: never
    include-binding-errors: never
    include-stacktrace: never
    include-exception: false

app:
  invoices:
    overdue-update:
      enabled: ${INVOICE_OVERDUE_UPDATE_ENABLED:true}
      cron: ${INVOICE_OVERDUE_UPDATE_CRON:0 5 0 * * *}
  bootstrap:
    admin:
      enabled: ${INITIAL_ADMIN_ENABLED:false}
      email: ${INITIAL_ADMIN_EMAIL:}
      password: ${INITIAL_ADMIN_PASSWORD:}
      first-name: ${INITIAL_ADMIN_FIRST_NAME:Administrador}
      last-name: ${INITIAL_ADMIN_LAST_NAME:ERP}
  performance:
    static-cache-days: ${STATIC_CACHE_DAYS:30}
  security:
    remember-me:
      enabled: ${REMEMBER_ME_ENABLED:false}
      key: ${REMEMBER_ME_KEY:}
    login-rate-limit:
      enabled: ${LOGIN_RATE_LIMIT_ENABLED:true}
      max-attempts: ${LOGIN_RATE_LIMIT_MAX_ATTEMPTS:10}
      window-minutes: ${LOGIN_RATE_LIMIT_WINDOW_MINUTES:15}
      max-tracked-entries: ${LOGIN_RATE_LIMIT_MAX_TRACKED_ENTRIES:10000}

management:
  endpoint:
    health:
      show-details: never
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    org.springframework.security: INFO
    org.hibernate.SQL: WARN
